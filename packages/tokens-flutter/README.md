# tokens-flutter

Flutter port of `@zunialab/tokens`.

`ui-native` remains for potential React Native surfaces; **zunia-mobile uses Flutter** and should depend on this package (path or git).

```yaml
dependencies:
  zunia_tokens:
    path: ../zunia-ui/packages/tokens-flutter
```
